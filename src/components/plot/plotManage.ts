/**
 * ---------------------------------------------
 * [Feature]: 텃밭 관리 화면이 공유하는 상수
 *
 * [Description]
 * - 목록(`PlotManageList`)과 하단 독(`PlotDeleteDock`)이 **같은 이름**을 봐야
 *   겨냥과 확인이 이어진다. 한쪽만 고치면 독이 영영 안 열리거나, 열려도 아무
 *   밭도 지우지 않는 상태가 된다.
 * - 지시자 없는 평범한 모듈이다. 서버 컴포넌트가 읽으므로 `"use client"` 파일에
 *   두면 안 된다(`bottomTabsLayout.ts` 주석의 함정).
 * ---------------------------------------------
 */

/**
 * 삭제를 겨냥한 라디오가 공유하는 클래스.
 *
 * 독은 "**어느** 밭이 겨냥됐나"가 아니라 "하나라도 겨냥됐나"만 알면 되므로
 * id 가 아니라 이 클래스를 본다(`group-has-[.arm:checked]/plots:`). 덕분에 밭이
 * 몇 개든 클래스를 새로 만들 필요가 없다 — id 로 하면 밭마다 클래스가 달라져
 * Tailwind 가 정적으로 읽지 못하고 **조용히 사라진다.**
 */
export const ARM_CLASS = "arm";

/** 삭제 폼의 id. 겨냥 라디오와 독의 제출 버튼이 `form` 속성으로 이 폼을 가리킨다. */
export const DELETE_FORM_ID = "plot-delete-form";

/** 아무것도 겨냥하지 않은 상태를 들고 있는 라디오. '취소'가 이걸 고른다. */
export const ARM_NONE_ID = "arm-none";
